{% extends 'base.html.twig' %}

{% block title %}Modeles{% endblock %}

{% block body %}

    <table class="table">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Nom</th>
                <th scope="col"> </th>
            </tr>
        </thead>
        <tbody>
        {% for modele in modeles %}
            <tr>
                <td scope="row">{{modele.id}}</td>
                <td>{{modele.libelle}}</td>
                 <td>{{modele.marque.libelle}}</td>
                <td >
                   {#} <a href="{{path('marque_show',{'id': marque.id})}}" class="btn btn-primary">Détails</a>
                    <a href="{{path('marque_edit',{'id': marque.id} )}}" class="btn btn-primary">Modifier</a>
                    <a href="{{path('marque_delete',{'id': marque.id} )}}" class="btn btn-primary">Supprimer</a></td>
            </tr>#}
        {% endfor %}
            
        </tbody>
    </table>

    <a href="{{path('modele_create')}}" class="btn btn-primary">Ajouter un modele</a>


{% endblock %}